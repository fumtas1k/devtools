Radio button — group several with a shared `name`; only one stays selected.

```jsx
<Radio name="pref" defaultChecked>東京都</Radio>
<Radio name="pref">大阪府</Radio>
```

- Pass label text as children. `isError` / `disabled` behave like Checkbox.
