Status pill — a compact filled label for record or process state.

```jsx
<StatusBadge tone="success">完了</StatusBadge>
<StatusBadge tone="warning">確認中</StatusBadge>
<StatusBadge>下書き</StatusBadge>
```

- `tone`: `neutral` (default grey) · `info` · `success` · `warning` · `error`.
