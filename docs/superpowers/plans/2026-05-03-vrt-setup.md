# VRT 専用 PR 導入 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Visual Regression Test (VRT) 基盤を独立 PR で proper sequencing（mock 注入 → CI Linux baseline 撮影 → 非 required check 化）で導入する。`#176` B 案 ui migration の visual change を CI で検出可能にする infra のみ、production code 変更ゼロ。

**Architecture:** Playwright `projects:` を 2 つ (`e2e` と `visual-regression`) に分離し、専用 spec (`tests/e2e/visual-regression.spec.ts`) で `addInitScript` 経由の deterministic mock を注入。専用 workflow (`visual-regression.yml`) が PR trigger で diff を PR comment + artifact 報告。`update-visual-baseline.yml` で baseline 再生成 (workflow_dispatch、default branch guard 付き)。VRT は branch protection の required check に **含めない**（user 手動設定）。

**Tech Stack:** Playwright (`@playwright/test`)、GitHub Actions (`actions/checkout@v4`, `actions/setup-node@v4`, `actions/upload-artifact@v4`, `peter-evans/create-or-update-comment`)、Node.js 22。

---

## 重要な前提

- spec: `docs/superpowers/specs/2026-05-03-vrt-setup-design.md` を必読
- 旧 PR #253（B 案 PR 1 で VRT を bundle した試み）が close 済み。本 PR は infra のみで再起動
- worktree: `worktrees/vrt-setup/` (既存)
- branch: `feature/vrt-setup` (既存)
- production code（`src/`）に変更を入れない
- 既存 e2e (144 + 1 skip) が壊れないこと

---

## ファイル構成

| 種別   | パス                                                        | 役割                                                                                |
| ------ | ----------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| Create | `tests/e2e/visual-regression.spec.ts`                       | 18 ページ × 2 viewport の screenshot test、`addInitScript` mock 注入                |
| Create | `tests/e2e/visual-regression.spec.ts-snapshots/*-linux.png` | CI Linux runner で生成した baseline（36 件、bot commit）                            |
| Modify | `playwright.config.ts`                                      | `chromium` project を `e2e` にリネーム + `visual-regression` project 追加           |
| Modify | `package.json`                                              | `test:e2e` script を `--project=e2e` に変更、`test:vrt` script + `pretest:vrt` 追加 |
| Create | `.github/workflows/visual-regression.yml`                   | PR trigger、build → VRT 実行 → PR comment + artifact upload                         |
| Create | `.github/workflows/update-visual-baseline.yml`              | `workflow_dispatch` + 一時 push trigger（bootstrap 用）、default branch guard       |
| Modify | `docs/playbooks/e2e-validation.md`                          | VRT 運用記述追加（diff 出たら reviewer がどう判断するか / baseline 更新方法）       |
| Modify | `docs/decisions.md`                                         | 新規 [066] エントリ追加（VRT architecture 採用根拠 + 失敗事例 PR #253 記録）        |

---

## Task 1: `playwright.config.ts` を 2 project に分離 + `package.json` script 更新

**目的**: VRT を通常 e2e から分離する infra 整備。`test:e2e` は VRT を実行しない / `test:vrt` で VRT 専用実行。

**Files:**

- Modify: `playwright.config.ts:18-23`
- Modify: `package.json:11-12`（`pretest:e2e` / `test:e2e` 行 + 追加）

- [ ] **Step 1: 現状確認**

```bash
sed -n '18,23p' playwright.config.ts
grep -n '"test:e2e"\|"pretest:e2e"' package.json
```

期待: `playwright.config.ts` の `projects:` に `chromium` 1 件のみ。`package.json` に `pretest:e2e` と `test:e2e` 各 1 行。

- [ ] **Step 2: `playwright.config.ts` を 2 project に書き換え**

`projects:` ブロック (L18-23) を以下に置換:

```ts
  projects: [
    // #VRT: 通常の E2E テスト用（visual regression spec を除外）
    {
      name: 'e2e',
      use: { ...devices['Desktop Chrome'] },
      testIgnore: ['**/visual-regression.spec.ts'],
    },
    // #VRT: visual regression test 専用（spec 限定、mock 注入は spec 内 addInitScript で実施）
    {
      name: 'visual-regression',
      use: { ...devices['Desktop Chrome'] },
      testMatch: ['**/visual-regression.spec.ts'],
    },
  ],
```

- [ ] **Step 3: `package.json` の scripts を更新**

`scripts:` 内の `pretest:e2e` / `test:e2e` を以下に変更し、`pretest:vrt` / `test:vrt` を追加:

```json
"pretest:e2e": "lsof -ti:4321 | xargs kill -9 2>/dev/null || true",
"test:e2e": "playwright test --project=e2e",
"pretest:vrt": "lsof -ti:4321 | xargs kill -9 2>/dev/null || true",
"test:vrt": "playwright test --project=visual-regression",
```

- [ ] **Step 4: 既存 e2e が想定通り動くことを確認（VRT spec 不在なので 144 + 1 skip のはず）**

```bash
npm run pretest:e2e
npm run test:e2e -- --workers=1 --reporter=list 2>&1 | tail -10
```

期待: 144 passed / 1 skipped / 0 failed。`visual-regression.spec.ts` がまだ存在しないので、項目数は変わらない。

- [ ] **Step 5: コミット**

```bash
git add playwright.config.ts package.json
git commit -m "chore(test): playwright config を e2e / visual-regression の 2 project に分離 + npm scripts 整備（VRT 導入準備）"
```

---

## Task 2: `tests/e2e/visual-regression.spec.ts` 新設（mock 注入版、baseline なし）

**Files:**

- Create: `tests/e2e/visual-regression.spec.ts`

- [ ] **Step 1: 新規ファイル作成**

```ts
import { expect, test } from '@playwright/test';
import { waitForReactHydration } from './helpers';

/**
 * Visual Regression Test (VRT) — `#176` B 案 ui migration の visual change を CI で検出する。
 *
 * 全 18 ページ × 2 viewport (Desktop 1280×800 / Mobile 390×844) = 36 screenshot を
 * baseline 比較する。production code には一切手を入れず、`addInitScript` で
 * `Math.random` / `crypto.randomUUID` / `Date.now` を deterministic に固定して non-determinism を排除。
 *
 * 運用:
 * - baseline は CI Linux runner で生成（mac とのフォントレンダリング差を回避）
 * - diff が出たら専用 workflow が PR comment で reviewer に報告
 * - 意図的な visual 変更なら `update-visual-baseline.yml` を workflow_dispatch で trigger して baseline 更新
 * - 意図しない regression なら該当変更を fix
 * - VRT は **required check に含めない**（意図的変更が merge を block しない設計）
 *
 * 詳細: docs/superpowers/specs/2026-05-03-vrt-setup-design.md
 */

const PAGES = [
  '/',
  '/about',
  '/privacy',
  '/tools/ulid-generator',
  '/tools/uuid-v7',
  '/tools/dummy-text',
  '/tools/qr-code',
  '/tools/jan-code',
  '/tools/gs1-databar',
  '/tools/qr-ticket',
  '/tools/qr-reader',
  '/tools/url-encode',
  '/tools/jwt-decoder',
  '/tools/base64',
  '/tools/json-xml',
  '/tools/json-csv',
  '/tools/encoding-converter',
  '/tools/config-converter',
] as const;

const VIEWPORTS = [
  { name: 'desktop', width: 1280, height: 800 },
  { name: 'mobile', width: 390, height: 844 },
] as const;

for (const viewport of VIEWPORTS) {
  test.describe(`visual regression - ${viewport.name} (${viewport.width}x${viewport.height})`, () => {
    test.use({ viewport });

    for (const url of PAGES) {
      test(`${url} の screenshot が baseline と一致`, async ({ page }) => {
        // navigation 前に deterministic mock を注入（production code 無変更）
        await page.addInitScript(() => {
          // Seeded LCG: Math.random を固定 seed (42) から再現可能な乱数列に
          let seed = 42;
          Math.random = () => {
            seed = (seed * 1103515245 + 12345) & 0x7fffffff;
            return seed / 0x7fffffff;
          };

          // Incremental UUID counter: 00000000-0000-0000-0000-NNNNNNNNNNNN 形式に
          let uuidCounter = 0;
          if (window.crypto) {
            const fixedUuid = (): `${string}-${string}-${string}-${string}-${string}` => {
              uuidCounter++;
              const n = uuidCounter.toString().padStart(12, '0');
              return `00000000-0000-0000-0000-${n}` as `${string}-${string}-${string}-${string}-${string}`;
            };
            window.crypto.randomUUID = fixedUuid;
          }

          // Fixed Date.now: 2026-01-01T00:00:00Z で固定
          const FIXED_NOW = 1767225600000;
          Date.now = () => FIXED_NOW;
        });

        await page.goto(url);
        // ハイドレーション完了を待ってから screenshot
        // （静的ページは hydration 対象がないため timeout する → catch でスキップ）
        await waitForReactHydration(page).catch(() => {
          /* about / privacy 等の静的ページは無視 */
        });

        await expect(page).toHaveScreenshot({
          fullPage: true,
          maxDiffPixels: 100,
          // フォントレンダリングの微差を許容
          maxDiffPixelRatio: 0.001,
        });
      });
    }
  });
}
```

- [ ] **Step 2: spec が project 分離下で認識されることを確認**

```bash
npx playwright test --list --project=visual-regression 2>&1 | tail -10
```

期待: 36 件の test が列挙される（18 ページ × 2 viewport）。

- [ ] **Step 3: 通常 e2e に混ざっていないことを確認**

```bash
npx playwright test --list --project=e2e 2>&1 | grep -c "visual-regression"
```

期待: 0（visual-regression spec が e2e project から除外されている）。

- [ ] **Step 4: 現時点では baseline 不在で fail するため commit のみ（baseline 生成は Task 5 で）**

```bash
git add tests/e2e/visual-regression.spec.ts
git commit -m "test(vrt): visual-regression spec を追加（addInitScript で deterministic mock 注入、baseline は後続 task で生成）"
```

---

## Task 3: `update-visual-baseline.yml` workflow を新設（bootstrap 用 push trigger 含む）

**目的**: workflow_dispatch だけでは default branch にしか workflow yml が無いと feature branch から trigger できない（GitHub Actions の制約）。本 PR の bootstrap として **一時 push trigger** で self-trigger させ、初回 baseline 生成完了後に push trigger を別 commit で削除する（PR #247 で確立した運用パターン）。

**Files:**

- Create: `.github/workflows/update-visual-baseline.yml`

- [ ] **Step 1: workflow yml を新規作成（bootstrap 用 push trigger 含む版）**

```yaml
name: Update Visual Regression Baseline

on:
  # #VRT: feature/vrt-setup ブランチから self-trigger するための一時 push trigger。
  # 初回 baseline 生成後、本 push trigger は別 commit で削除し workflow_dispatch のみに戻す。
  # （GitHub Actions 制約: workflow_dispatch は default branch からしか trigger できないため、
  # 初回ブートストラップにのみ push trigger を使う。PR #247 で確立した運用）
  push:
    branches:
      - feature/vrt-setup
    paths:
      - .github/workflows/update-visual-baseline.yml
      - tests/e2e/visual-regression.spec.ts
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-baseline:
    # default branch (develop / main) 上で誤って trigger された場合は no-op。
    # branch protection 違反を回避し、「baseline 更新は必ず PR ブランチで」を workflow レベルで強制。
    if: github.ref != 'refs/heads/develop' && github.ref != 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Playwright ブラウザをインストール
        run: npx playwright install --with-deps chromium
      - name: 本番相当アセットを build
        run: npm run build
      - name: Visual regression baseline を再生成
        run: npx playwright test --project=visual-regression --workers=1 --update-snapshots
      - name: 変更があれば commit & push
        run: |
          if [ -n "$(git status --porcelain tests/e2e/visual-regression.spec.ts-snapshots/)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add tests/e2e/visual-regression.spec.ts-snapshots/
            git commit -m "test(vrt): visual regression baseline を CI Linux runner で再生成"
            git push
          else
            echo "baseline 差分なし、commit skip"
          fi
```

- [ ] **Step 2: yml syntax 検証**

```bash
node -e "const yaml = require('js-yaml'); yaml.load(require('fs').readFileSync('.github/workflows/update-visual-baseline.yml','utf8')); console.log('valid')"
```

期待: `valid`。

- [ ] **Step 3: コミット**

```bash
git add .github/workflows/update-visual-baseline.yml
git commit -m "ci(vrt): update-visual-baseline workflow を新設（bootstrap 用 push trigger + workflow_dispatch + default branch guard）"
```

---

## Task 4: `visual-regression.yml` workflow を新設（PR trigger、PR comment + artifact）

**Files:**

- Create: `.github/workflows/visual-regression.yml`

- [ ] **Step 1: workflow yml を新規作成**

```yaml
name: Visual Regression

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

# 同一 PR への新しい push が来たら実行中の job をキャンセル
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write # PR comment 投稿用

jobs:
  visual-regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Playwright ブラウザをインストール
        run: npx playwright install --with-deps chromium

      - name: 本番相当アセットを build
        run: npm run build

      - name: VRT 実行（fail しても後続 step を続行）
        id: vrt
        continue-on-error: true
        run: npm run test:vrt -- --workers=1

      - name: テストレポートを artifact upload（pass/fail 問わず）
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: visual-regression-report-pr-${{ github.event.pull_request.number }}
          path: playwright-report/
          retention-days: 14

      - name: 結果を PR comment で報告
        if: always()
        uses: peter-evans/create-or-update-comment@71345be0265236311c031f5c7866368bd1eff043 # v4.0.0
        with:
          issue-number: ${{ github.event.pull_request.number }}
          body-includes: '## 🖼️ Visual Regression Test 結果'
          body: |
            ## 🖼️ Visual Regression Test 結果

            - **Status**: ${{ steps.vrt.outcome == 'success' && '✅ 全 36 件 pass' || '❌ 一部 fail（詳細は artifact 参照）' }}
            - **Workflow run**: [${{ github.run_id }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
            - **Artifact (diff 画像 / playwright-report)**: 上記 workflow run の `Artifacts` セクションから download

            > diff が **意図的な visual 変更**の場合: `Update Visual Regression Baseline` workflow を本 PR ブランチで `workflow_dispatch` trigger して baseline を更新。
            > diff が **意図しない regression** の場合: 該当変更を fix。
            > 本 check は **required ではない**ため fail のままでも merge は可能（reviewer 判断）。
          edit-mode: replace

      - name: VRT 失敗を job の最終 conclusion に反映（continue-on-error の補正）
        if: steps.vrt.outcome == 'failure'
        run: exit 1
```

> 注: `peter-evans/create-or-update-comment` の SHA pin は v4.0.0 の commit SHA。最新版を使う場合は別途調査して更新。

- [ ] **Step 2: yml syntax 検証**

```bash
node -e "const yaml = require('js-yaml'); yaml.load(require('fs').readFileSync('.github/workflows/visual-regression.yml','utf8')); console.log('valid')"
```

期待: `valid`。

- [ ] **Step 3: コミット**

```bash
git add .github/workflows/visual-regression.yml
git commit -m "ci(vrt): visual-regression workflow を新設（PR trigger、PR comment + artifact、continue-on-error で non-blocking）"
```

---

## Task 5: 親対応 — push して bootstrap で baseline 生成

**Files:** 触らない（push と GitHub 操作のみ）

- [ ] **Step 1: 親が push**（subagent は push しない）

```bash
git push -u origin feature/vrt-setup
```

- [ ] **Step 2: `update-visual-baseline.yml` の push trigger が `paths` フィルタに合致して自動起動する**

`paths:` には `.github/workflows/update-visual-baseline.yml` と `tests/e2e/visual-regression.spec.ts` を含めているため、本 push（Task 1-4 の commit を含む）で trigger される想定。

```bash
sleep 30
gh run list --workflow="Update Visual Regression Baseline" --branch feature/vrt-setup --limit 3 --json status,conclusion,databaseId --jq '.'
```

期待: workflow run が `in_progress` か `completed -> success` で記録されている。

- [ ] **Step 3: workflow 完了を Monitor で待機**

```bash
prev=""
while true; do
  s=$(gh run list --workflow="Update Visual Regression Baseline" --branch feature/vrt-setup --limit 1 --json status,conclusion,databaseId 2>/dev/null)
  cur=$(jq -r '.[0] | "\(.databaseId): \(.status) -> \(.conclusion // "pending")"' <<<"$s" 2>/dev/null)
  if [ -n "$cur" ] && [ "$cur" != "$prev" ]; then echo "$cur"; prev="$cur"; fi
  if echo "$cur" | grep -qE "completed -> (success|failure|cancelled|timed_out)"; then break; fi
  sleep 20
done
```

期待: 10 分以内に `completed -> success`。bot が baseline 36 件を commit & push back。

- [ ] **Step 4: baseline を local に pull**

```bash
git pull --ff-only origin feature/vrt-setup
ls tests/e2e/visual-regression.spec.ts-snapshots/ | wc -l
ls tests/e2e/visual-regression.spec.ts-snapshots/ | grep -c "linux"
```

期待: 36 ファイル（すべて `*-linux.png`）。

---

## Task 6: 親対応 — `update-visual-baseline.yml` から push trigger を削除

**Files:**

- Modify: `.github/workflows/update-visual-baseline.yml`

- [ ] **Step 1: workflow yml の `on:` を `workflow_dispatch:` のみに戻す**

該当部分を以下に置換:

```yaml
on:
  # default branch にマージ後は workflow_dispatch (手動 trigger) でのみ実行する。
  # baseline を意図的に再生成したい時（design system 変更、フォント更新等）に Actions UI から手動起動する想定。
  # 注意: workflow_dispatch は workflow 定義が default branch (develop) に存在することが前提。
  # 本 PR merge 後に有効化される。
  workflow_dispatch:
```

- [ ] **Step 2: 検証**

```bash
node -e "const yaml = require('js-yaml'); yaml.load(require('fs').readFileSync('.github/workflows/update-visual-baseline.yml','utf8')); console.log('valid')"
grep -c "feature/vrt-setup" .github/workflows/update-visual-baseline.yml
```

期待: `valid` + 0 件（push trigger / branch 指定が完全に削除されている）。

- [ ] **Step 3: コミット**

```bash
git add .github/workflows/update-visual-baseline.yml
git commit -m "ci(vrt): update-visual-baseline workflow から bootstrap 用 push trigger を削除し workflow_dispatch のみに戻す"
```

> 注: この commit は `paths:` フィルタの対象外（workflow yml だけの変更で `paths:` に含まれる）になるが、push trigger 削除の commit 自体が trigger を引くと無限ループ気味になる。実際には trigger は走っても workflow が前回と同じ baseline を再生成するだけなので無害。気になる場合は GitHub Actions UI で確認して手動 cancel。

---

## Task 7: docs 更新 — `e2e-validation.md` + `decisions.md` [066]

**Files:**

- Modify: `docs/playbooks/e2e-validation.md`
- Modify: `docs/decisions.md`

### Task 7a: `e2e-validation.md` に VRT 運用記述を追加

- [ ] **Step 1: 現状確認**

```bash
grep -n "## " docs/playbooks/e2e-validation.md | head -10
```

- [ ] **Step 2: 末尾に新セクション追加**

`docs/playbooks/e2e-validation.md` の末尾に以下を append:

````markdown
---

## 7. Visual Regression Test (VRT)

**いつ読むか**: ui コンポーネント / page layout の visual 変更を伴う PR を作成 / レビューする時。

### 7.1 VRT の位置付け

- 実行: 専用 workflow `Visual Regression`（PR trigger）が `npm run test:vrt` を起動
- 通常 e2e (`npm run test:e2e`) は VRT を実行しない（playwright project 分離による）
- VRT は **required check に含まれない**（意図的 visual 変更が merge を block しない設計）
- 結果: PR comment と artifact (`playwright-report`) で報告
- baseline 配置: `tests/e2e/visual-regression.spec.ts-snapshots/*-linux.png` (CI Linux runner で生成)

### 7.2 PR comment に diff が出た時の判断フロー

1. PR comment 内の `Workflow run` リンクから diff 画像を確認
2. **意図的な visual 変更**（design system update / 新 component 追加 / 意図的 layout 調整）の場合:
   - GitHub Actions UI で `Update Visual Regression Baseline` workflow を **本 PR のブランチ** で `workflow_dispatch` trigger
   - bot が baseline を更新して commit back
   - `git pull` で baseline を receive、push（branch protection は feature branch なので問題なし）
   - VRT が pass し PR comment が「✅ 全 36 件 pass」に更新される
3. **意図しない visual 変更**（regression）の場合:
   - 該当変更を fix
   - VRT が pass するまで commit & push を repeat
4. 判断保留で merge したい場合:
   - VRT は required ではないので fail のままでも merge は可能
   - ただし develop の baseline と乖離した状態で merge すると後続 PR で diff が大量発生するため、極力避ける

### 7.3 VRT のローカル実行

```bash
npm run test:vrt
```
````

> ⚠️ ローカル mac で実行すると CI Linux baseline と OS 差で fail する。**ローカル baseline (`*-darwin.png`) は通常 commit しない**。development 用に残す場合のみ `--update-snapshots` で生成（ただし git に commit しないこと）。CI 上の Linux baseline (`*-linux.png`) のみが SoT。

### 7.4 VRT 自体の architecture 変更（rare ops）

- spec は `tests/e2e/visual-regression.spec.ts`
- mock は `addInitScript` で `Math.random` / `crypto.randomUUID` / `Date.now` を固定（spec 上部参照）
- 新 page を VRT 対象に追加: spec の `PAGES` 配列に path 追記 → baseline 再生成
- 新 viewport 追加: `VIEWPORTS` 配列に追記 → baseline 再生成
- 詳細: `docs/decisions.md` [066]

````

### Task 7b: `decisions.md` [066] エントリを追加

- [ ] **Step 1: 既存末尾を確認**

```bash
tail -10 docs/decisions.md
````

- [ ] **Step 2: 末尾に [066] エントリを append**

```markdown
---

## [066] 2026-05-03 — VRT (Visual Regression Test) を独立 PR + 専用 workflow + 非 required check で導入

**2026-05-03 | ステータス: 採用**

### 背景

`#176` B 案 ui migration (`style={{}}` 200+ 箇所の className 化) に向けた visual regression 検出基盤として VRT を導入する。当初は ui migration と同じ PR で導入を試みた（旧 PR #253）が、以下 3 つの構造的問題で破綻し close:

1. **VRT setup を feature work と bundle した結果 infra 設計が後回し**: deterministic mock の注入タイミング、baseline 撮影タイミング、required check 化、すべてが場当たり的になった
2. **mock を最初から組まなかった**: baseline が non-deterministic な状態で撮影され、後付けで mock を入れても baseline 側が古いまま flake が継続。構造的に fix 不可能
3. **VRT を required check 想定で作った**: 意図的 visual 変更（例: BareInput の `mono` prop 由来の system mono → JetBrains Mono web font 移行）のたびに merge ブロックが発生する運用 friction

旧 PR #253 のレビュー過程で「修正前の状態でランダムを固定して snapshot を取る必要がある」「修正を意図的にした場合は、こけてても通す必要があるから必須テストに入れてはダメ」とユーザー指摘あり。完全に正当な architectural critique のため close した。

### 決断

VRT を **独立 PR (本 PR)** で先行導入し、以下の 5 つの設計原則を最初から適用する:

1. **Playwright project 分離**: `playwright.config.ts` で `e2e` (通常テスト) と `visual-regression` (VRT 専用) に分離。通常 `npm run test:e2e` は VRT を実行しない
2. **Deterministic mock 注入**: spec 内 `page.addInitScript()` で `Math.random` (seeded LCG) / `crypto.randomUUID` (incremental counter) / `Date.now` (固定 timestamp) を navigation 前に decorate。production code 無変更
3. **baseline は CI Linux で生成**: `update-visual-baseline.yml` workflow が CI runner で `--update-snapshots` を実行、bot が同 branch に commit back。ローカル mac の OS 差を排除
4. **専用 workflow + PR comment + artifact**: `visual-regression.yml` が PR trigger で VRT 実行、結果を PR comment（pass/fail サマリ + artifact link）で報告、`continue-on-error: true` で job 単体は fail しても workflow_run としては記録される
5. **branch protection の required check に含めない**: GitHub Settings UI で user が手動設定。意図的 visual 変更が merge ブロックしない設計

加えて `update-visual-baseline.yml` には **default branch guard** (`if: github.ref != 'refs/heads/develop' && github.ref != 'refs/heads/main'`) を入れ、誤って develop / main 上で trigger されても no-op となり branch protection 違反を回避。

### 副次効果 / 移行

- 旧 PR #253 で得た 3 件のメモリ (`feedback_vrt_setup_sequencing.md` / `feedback_subagent_verification_trust.md` / `feedback_infra_feature_separation.md`) を保存済み。同じ構造的失敗の再発を抑止
- 後続 ui migration PR (B 案 PR 1〜PR 6) は本 PR の VRT 監視下で実施。意図的変更は baseline 更新で accept、意図しない regression は fix
- `tests/e2e/visual-regression.spec.ts` の `addInitScript` mock 範囲は将来追加 page で不足する可能性あり（新 page で別の non-deterministic API を使う場合）。発見次第 mock を拡張する運用

### 却下した選択肢

- **VRT を ui migration と同 PR で導入**: 旧 PR #253 で破綻した。
- **VRT を required check に含める**: 意図的 visual 変更のたびに merge friction が発生し、reviewer の判断機会を奪う。non-required + PR comment + workflow_dispatch baseline 更新の組み合わせがバランス良い
- **mask で動的領域を screenshot から除外**: 表面的な解決にとどまる。`addInitScript` で source の non-determinism を断つ方が clean
- **darwin baseline も commit**: ローカル mac DX が改善するが、Linux baseline と乖離した時の混乱が大きい。CI Linux baseline を SoT に固定し、ローカル diff は無視する運用が clean

### 関連 PR / issue

- 本 PR: 実装時に番号置換
- 失敗 PR: [#253](https://github.com/fumtas1k/devtools/pull/253) (closed)
- 起源: `#176` B 案（`style-src 'unsafe-inline'` 削減）の前提整備
- 過去: [063] (preview 切替), [064] (CSP A-1), [065] (webServer CI 分岐)
- 後続: B 案 PR 1（基礎工事 + ui/\* simple 11 ファイル migration）から再着手
```

- [ ] **Step 3: docs-references vitest を実行**

```bash
npm run test -- tests/meta/docs-section-references.test.ts 2>&1 | tail -5
```

期待: 7/7 pass。`e2e-validation.md` に新セクション 7 を追加した影響で章番号 drift がないことを確認。

- [ ] **Step 4: コミット（docs 一括）**

```bash
git add docs/playbooks/e2e-validation.md docs/decisions.md
git commit -m "docs(vrt): VRT 運用記述を e2e-validation.md に追加 + decisions.md [066] で採用根拠と PR #253 失敗事例を記録"
```

---

## Task 8: 親対応 — 最終検証 + PR 作成 + branch protection 注意 PR description 明記

**Files:** 触らない

- [ ] **Step 1: develop ベース確認**

```bash
git rev-parse origin/develop
git merge-base HEAD origin/develop
```

期待: 一致。

- [ ] **Step 2: 全 unit + 型 + docs-references**

```bash
npm run test 2>&1 | tail -5
node_modules/.bin/astro check 2>&1 | tail -5
npm run test -- tests/meta/docs-section-references.test.ts 2>&1 | tail -5
```

期待: 全 pass / 0 errors。

- [ ] **Step 3: 通常 e2e（VRT 除外）が pass**

```bash
npm run pretest:e2e
npm run test:e2e -- --workers=1 2>&1 | tail -5
```

期待: 144 passed / 1 skipped / 0 failed（VRT 含まれない、所要時間も従来通り）。

- [ ] **Step 4: VRT を local で 2 回連続実行して deterministic 確認**

```bash
# 1 回目（既存 baseline = CI Linux baseline と darwin で OS 差で fail する可能性あり）
npm run pretest:vrt
npm run test:vrt -- --workers=1 2>&1 | tail -5

# 重要: ローカル fail は OS 差由来で正常な挙動。CI 上で確認済みであれば OK。
# 必要なら --update-snapshots でローカル darwin baseline を生成（ただし commit しない）
```

期待: ローカル mac では OS 差で fail する可能性あり（playbook L7.3 の通り）。CI 上での pass が SoT。

- [ ] **Step 5: scope 外 diff チェック**

```bash
git diff origin/develop --name-only
git diff origin/develop -- '*.tsx' '*.astro' | grep -E '^-.*aria-' || echo "OK: aria 削除なし"
```

想定変更ファイル:

- `playwright.config.ts`
- `package.json`
- `tests/e2e/visual-regression.spec.ts` (新)
- `tests/e2e/visual-regression.spec.ts-snapshots/*-linux.png` (新、36 件 / bot commit 由来)
- `.github/workflows/visual-regression.yml` (新)
- `.github/workflows/update-visual-baseline.yml` (新)
- `docs/playbooks/e2e-validation.md`
- `docs/decisions.md`
- `docs/superpowers/specs/2026-05-03-vrt-setup-design.md`
- `docs/superpowers/plans/2026-05-03-vrt-setup.md`

`src/` の変更は **無いべき**（infra のみの PR）。

- [ ] **Step 6: PR 作成（`--base develop` 必須）**

PR 本文ファイル: `$TMPDIR/pr-vrt-setup.md`。本文には以下を必ず含める:

- 概要（VRT 独立導入、旧 PR #253 close 経緯）
- 主な変更（playwright config / spec / workflow × 2 / docs）
- 検証結果
- ⚠️ **branch protection 設定の注意**: ユーザーが GitHub Settings UI から `Settings → Branches → Branch protection rules → develop → Require status checks → ` で **`visual-regression` を required checks の list に含めない** ことを必ず確認。check 自体は走るが、merge を block しない non-required 扱いとする
- 関連 spec / decisions / メモリ / 後続 PR

```bash
gh pr create --base develop \
  --title "ci(vrt): Visual Regression Test 基盤を独立 PR で導入（旧 #253 学習を踏まえた proper sequencing）" \
  --body-file "$TMPDIR/pr-vrt-setup.md"
```

- [ ] **Step 7: PR 番号を `decisions.md` [066] の `本 PR:` に置換**

PR 番号確定後（`pr-creation.md` 4.1 章のフロー）:

```bash
PR_NUM=$(gh pr view --json number --jq .number)
PR_URL=$(gh pr view --json url --jq .url)
sed -i.bak "s|本 PR: 実装時に番号置換|本 PR: [#${PR_NUM}](${PR_URL})|" docs/decisions.md
rm docs/decisions.md.bak
git add docs/decisions.md
git commit -m "docs(decisions): [066] の 本 PR 番号 (#${PR_NUM}) を置換"
git push
```

- [ ] **Step 8: PR URL を控える + メモリ `project_b_plan_progress.md` の進捗テーブルを更新**

メモリの「PR 0」行を「未着手 → レビュー中（PR #N）」に更新。

---

## 完了基準（PR マージ時）

- [ ] `playwright.config.ts` が `e2e` / `visual-regression` 2 project に分離
- [ ] `package.json` に `test:vrt` / `pretest:vrt` script
- [ ] `tests/e2e/visual-regression.spec.ts` が `addInitScript` mock を含む
- [ ] `tests/e2e/visual-regression.spec.ts-snapshots/` に CI Linux baseline 36 件 commit 済み
- [ ] `.github/workflows/visual-regression.yml`（PR trigger、PR comment + artifact、continue-on-error）
- [ ] `.github/workflows/update-visual-baseline.yml`（workflow_dispatch only、default branch guard）
- [ ] `npm run test:e2e` が 144 + 1 skip pass（VRT 含まない）
- [ ] CI 上で VRT が連続 2 回以上 36/36 安定 pass（少なくとも本 PR の 2 回 push で確認）
- [ ] `docs/playbooks/e2e-validation.md` に VRT 運用記述追加
- [ ] `docs/decisions.md` [066] 追加 + 本 PR 番号置換
- [ ] PR description に「branch protection の required checks に visual-regression を含めない」を明記
- [ ] PR が `--base develop` で作成され、test + e2e check が green
- [ ] visual-regression check は green か（fail でも non-blocking なので merge 可能）
- [ ] メモリ `project_b_plan_progress.md` の PR 0 行が更新されている
