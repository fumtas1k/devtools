# ツール候補リスト

Agent Teams によるブレインストーミング（2026-04-29）の結果を記録する。
優先度は「ブラウザ完結である必然性（機密データを外部送信しないメリット）」「実装難度」「既存サービスとの差別化」の3軸で評価。

> **状態列**: 実装済みの候補は各表の「状態」列に ✅ と追加 PR 番号を記載する。ツール追加 PR のマージ時に更新すること（運用ルールは `.agents/rules/common.md` 5 章）。

## S tier ― ブラウザ完結である必然性が最も高い

| #   | 状態    | ツール名                           | slug（案）         | 概要                                                                                          | 技術メモ                                                                                                                                     |
| --- | ------- | ---------------------------------- | ------------------ | --------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| S-1 | ✅ #639 | **DSN / 接続文字列ビルダ＆パーサ** | `dsn-builder`      | `postgres://` `mysql://` `mongodb+srv://` `redis://` `amqp://` をフォーム⇄URIで双方向編集     | 接続文字列にはパスワードが必ず含まれる → 外部送信不可が必然。`URL` API＋ドライバ別パラメータ辞書                                             |
| S-2 | ✅ #643 | **SSL/TLS 証明書解析**             | `cert-decoder`     | PEM/DER/PKCS#7 を貼り付け → Subject/SAN/有効期限/署名アルゴリズム/SCTを表示。チェーン検証対応 | 社内CA・本番証明書を外部送信不可の現場向け。`pkijs` + `asn1js` + Web Crypto API を採用。PKCS#12 は #644、鍵フォーマット変換は ✅ #645 へ分離 |
| S-3 |         | **Protobuf バイナリデコーダ**      | `protobuf-decoder` | `.proto` をアップロードして hex/base64 ペイロードをデコード。ワイヤレベル可視化も可能         | `.proto` 自体が社外秘のことが多い。`protobufjs` で動的コンパイル（import解決はユーザの一括アップロード前提）                                 |

## A tier ― 高頻度・差別化明確

| #   | 状態    | ツール名                              | slug（案）         | 概要                                                                         | 技術メモ                                                                                  |
| --- | ------- | ------------------------------------- | ------------------ | ---------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| A-1 | ✅ #490 | **正規表現ビジュアライザ＆ReDoS検出** | `regex-visualizer` | Railroad図表示・複数テストケース実行・catastrophic backtracking検出を一体化  | `regexp-tree` で AST→SVG、ReDoS 静的解析。PII含む文字列でも安全                           |
| A-2 | ✅ #133 | **設定ファイル相互変換**              | `config-converter` | YAML⇄JSON⇄TOML⇄.env⇄HCL を相互変換。コメント保持・`$schema` 検証対応         | IaC時代の必需品。本番設定・Terraform変数を外部送信不可の現場向け                          |
| A-3 |         | **Cron式プレイグラウンド**            | `cron-playground`  | Cron式を日本語翻訳、自然言語から生成、N回分の発火時刻を任意TZで一覧表示      | `cron-parser` + `cronstrue`（i18n）。5/6/7フィールド・Quartz/AWS EventBridge方言・JST対応 |
| A-4 |         | **コントラスト比マトリクス**          | `contrast-matrix`  | 任意のN色の全組合せ（N×N）コントラスト比を表示。WCAG 2.2 AA/AAA と APCA 併記 | 純粋計算（CIE Luminance, APCA公式アルゴリズム）。ブランドカラー外部送信不要               |
| A-5 | ✅ #422 | **TOTP/HOTP ジェネレータ＆検証**      | `totp-hotp`        | TOTP/HOTPコードの生成・検証。2FA実装のデバッグ補助                           | Web Crypto API（HMAC-SHA1/SHA-256/SHA-512）。TOTP seedを外部送信しない必然性が高い        |

## B tier ― 独自性・実用性あり

| #   | 状態    | ツール名                                      | slug（案）               | 概要                                                                                        | 技術メモ                                                                               |
| --- | ------- | --------------------------------------------- | ------------------------ | ------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| B-1 |         | **i18n メッセージカタログ整合性チェッカー**   | `i18n-checker`           | `*.json`/`*.yaml`/`*.po`/`*.arb` をD&D → キー欠落・ICUプレースホルダ不一致・翻訳率を可視化  | `@formatjs/icu-messageformat-parser`。VS Code拡張なしで使えるWebツールがほぼ存在しない |
| B-2 |         | **MessagePack/CBOR/BSON ビューア**            | `binary-codec-viewer`    | バイナリシリアライゼーション形式を JSON と相互変換し、バイト単位で構造を可視化              | `@msgpack/msgpack` / `cbor-x` / `bson`。3形式統合UIは希少                              |
| B-3 | ✅ #582 | **CIDR/サブネット計算機**                     | `cidr-calculator`        | IPv4+IPv6+VLSM対応。複数CIDR重複検出・サブネット分割テーブル生成                            | BigInt でIPv6完全対応。社内ネットワーク構成を外部計算機に貼れない現場向け              |
| B-4 |         | **ハッシュ計算＆HMAC**                        | `hash-calculator`        | MD5/SHA-256/SHA-512/BLAKE3、ファイルハッシュ、HMAC（任意キー）を計算                        | Web Crypto API。ファイル・文字列両対応                                                 |
| B-5 |         | **JSONPath/JMESPath/jq 並列プレイグラウンド** | `json-query`             | 同じ JSON 入力に対し3種クエリ言語の結果を並列比較                                           | JSONPath/JMESPath は純JS。jq は WASM ビルドで実現可能                                  |
| B-6 |         | **デザイントークン抽出ツール**                | `design-token-extractor` | CSS/SCSS/Tailwind config → W3C Design Tokens（JSON）＋ Style Dictionary 互換形式で出力      | `postcss` + `csstree`。社内デザインシステムの未公開トークンを外部送信しない            |
| B-7 |         | **SVG 最適化＆コンポーネント一括生成**        | `svg-optimizer`          | SVG を D&D → SVGO最適化 → React/Vue/Astroコンポーネント、SVGスプライト、data URLを一括出力  | `svgo/dist/svgo.browser.js` で動作                                                     |
| B-8 |         | **パスワードエントロピー＆強度チェッカー**    | `password-checker`       | パスワード強度・エントロピー計算。辞書攻撃耐性も可視化                                      | zxcvbn相当をブラウザ完結で。実パスワードを外部送信しない必然性が高い                   |
| B-9 |         | **Kubernetes Manifest 解析＆Lint**            | `k8s-lint`               | ツリー表示＋kube-score風ベストプラクティス検査（resource limits不足、runAsNonRoot未設定等） | Manifestに含まれる内部レジストリ情報を外部送信しない点が差別化                         |

## C tier ― 実用的だが既存サービスで代替可

| #   | ツール名                        | 概要                                                      | 備考                                          |
| --- | ------------------------------- | --------------------------------------------------------- | --------------------------------------------- |
| C-1 | UNIXタイムスタンプ&マルチTZ変換 | epoch⇄ISO8601、ナノ秒精度対応、バッチ変換                 | ナノ秒精度・JST対応で差別化可能               |
| C-2 | 画像一括最適化（WebP/AVIF変換） | 複数画像を一括変換。EXIF削除・`<picture>`タグ生成         | Squooshの多ファイル版。顧客素材の外部送信不要 |
| C-3 | JSON Diff / JSON Patch生成      | 2つのJSONを比較しRFC 6902/7396 Patchを生成・適用          | `fast-json-patch`。RFC準拠Patch生成で差別化可 |
| C-4 | CSP バリデータ＆ビジュアライザ  | Content Security Policyのディレクティブを視覚化・違反検出 | 純粋パーサーで実現可                          |
| C-5 | 型スキーマ相互変換              | TypeScript⇄Zod⇄JSON Schema⇄OpenAPIを双方向変換            | 技術難度Medium。`quicktype-core`等要調整      |

## 重複・統合メモ

- **Cron**: A-3 で DevOps/Backend の両視点を統合（7フィールド・複数方言・JST・夏時間対応を全部入れ）
- **SSL/TLS証明書**: S-2 に証明書解析＋鍵フォーマット変換（PEM/DER/JWK）を統合 → 鍵変換は ✅ #645 (`key-converter`) として分離実装済み
- **型スキーマ変換**: C-5 に統合（JSON Schema⇄TS⇄Zodに絞ると難度High化）

---

# 第2回ブレインストーミング（2026-06-11）

並列サブエージェント4体（バックエンド/インフラ・フロントエンド/デザイン・セキュリティ・日本の業務システム/データ整備）で発散し、重複統合のうえ第1回と同じ3軸で評価した。第1回候補・実装済みツールとの重複は除外。

**収束シグナル**: 「シークレット/PII マスキング系」は4視点中3視点が、「SAML デコーダ」は2視点が独立に提案しており、需要の確度が高い。

## S tier ― ブラウザ完結である必然性が最も高い

| #    | 状態    | ツール名                        | slug（案）        | 概要                                                                                                                                                             | 技術メモ                                                                                                                                                           |
| ---- | ------- | ------------------------------- | ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| S2-1 | ✅ #631 | **シークレット/ログマスキング** | `secret-scrubber` | ログ・コード・設定を貼ると API キー / トークン / JWT / メール / IP / 電話番号等を検出して一括マスク（同一値は同一プレースホルダ）。LLM や issue に貼る前の前処理 | 3視点が独立提案。gitleaks 系正規表現ルール＋エントロピー判定の純 JS で Medium。**検知器のため test-gates skill（陽性対照テスト）必須**                             |
| S2-2 |         | **SAML レスポンスデコーダ**     | `saml-decoder`    | `SAMLResponse` の base64 + deflate を展開し Assertion（NameID / 属性 / Conditions / Audience）をツリー表示。SSO トラブルの定番チェックを自動判定                 | Assertion は社員 PII の塊で samltool.com に貼れない。`pako` + `DOMParser` で Low〜Medium。XMLDSig 署名検証まで踏むと High（初版スコープ外可）                      |
| S2-3 |         | **CSV 匿名化・PII ダミー置換**  | `csv-anonymizer`  | CSV 列ごとに削除 / ハッシュ化 / 一貫ダミー置換（同一人物は同一偽名）/ 数値丸めを指定して匿名化版を出力。本番データから検証用データを作る定番作業                 | `papaparse` + Web Crypto。日本語ダミー値（氏名・住所）はダミーテキスト生成の資産を流用。S2-1 と検出ルール・辞書を共有可能                                          |
| S2-4 | ✅ #675 | **HAR ビューア＆サニタイザ**    | `har-viewer`      | DevTools の HAR を D&D で閲覧（一覧 / ウォーターフォール）し、Cookie / Authorization / トークン類を自動 redact した共有用 HAR を出力                             | HAR はセッション Cookie・POST ボディを丸ごと含む。HAR は単なる JSON でパース容易、ウォーターフォール UI は #674（PR #708）で追加済み。閲覧＋サニタイズ一体型は空白 |

## A tier ― 高頻度・差別化明確

| #    | 状態    | ツール名                                 | slug（案）                   | 概要                                                                                                                                             | 技術メモ                                                                                                                    |
| ---- | ------- | ---------------------------------------- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------- |
| A2-1 |         | **文字化け復元**                         | `mojibake-fixer`             | 化けたテキストをエンコード誤適用の組合せ総当たりで逆変換し、日本語らしさスコア順に復元候補を提示。MIME ヘッダデコード対応                        | `encoding-japanese` 再利用（文字コード判定・変換の兄弟ツール）。日本語特化で ftfy 系と明確に差別化。Medium                  |
| A2-2 |         | **日本語テキスト正規化**                 | `ja-text-normalizer`         | 全角半角・カナ・NFC/NFD 濁点結合・波ダッシュ（U+301C/U+FF5E）・スペースをルール組合せで一括正規化し、前後 diff を表示                            | `String.prototype.normalize` ＋自作マッピングの純ロジックで Low。氏名・口座名義等の PII が対象でブラウザ完結必然            |
| A2-3 |         | **CSV ドクター**                         | `csv-doctor`                 | 文字コード混在 / BOM / 列数不一致 / セル内改行 / Excel 0落ち・指数表記化（1.23E+15）を一括診断し修正版 CSV を出力                                | 「日本の CSV 病理」特化診断は空白地帯。`papaparse` + `encoding-japanese`。Medium                                            |
| A2-4 | ✅ #635 | **クリップボードインスペクタ**           | `clipboard-inspector`        | 貼り付けるとクリップボード上の全 MIME フレーバー（text/plain・text/html・image 等）と中身を可視化。サニタイズ後プレビュー                        | `paste` イベントのみで Low。クリップボード内容は原理的に 100% ブラウザ完結。リッチテキストエディタ開発の定番デバッグ需要    |
| A2-5 |         | **キーボード/IME イベントインスペクタ**  | `key-event-inspector`        | `keydown`〜`composition*` イベント列をタイムライン表示し、日本語 IME 変換中の発火順序を可視化                                                    | ライブラリ不要で Low。原理的にブラウザでしか実現できない。Enter 確定でフォーム送信される問題等、日本語圏の定番の悩みに直撃  |
| A2-6 |         | **Unicode 文字インスペクタ**             | `unicode-inspector`          | コードポイント / UTF-8・16 バイト列 / 書記素クラスタ境界 / 正規化差分を表示し、ゼロ幅・BiDi 制御文字・ホモグリフを警告                           | `Intl.Segmenter` + Unicode confusables 静的データで Low〜Medium。Trojan Source 検出というセキュリティ用途。検知部は陽性対照 |
| A2-7 |         | **鍵ペア整合性チェッカー＆SSH 鍵ツール** | `key-matcher`                | 証明書 / CSR / 秘密鍵 / SSH 公開鍵を複数貼ると公開鍵指紋でペア判定して表形化。鍵種別・fingerprint 表示・`authorized_keys` 生成・フォーマット変換 | `openssl -modulus` の diff 職人芸を置換。Web Crypto `importKey`/`exportKey`（SPKI 比較）+ `pkijs` / `@noble/*`。Low〜Medium |
| A2-8 |         | **Webhook 署名検証デバッガ**             | `webhook-signature-debugger` | Stripe / GitHub / Slack / LINE 等の HMAC 署名をプロバイダ別プリセットで再現し、期待署名の組み立て過程をステップ表示して不一致原因を診断          | Web Crypto（HMAC）のみで Low〜Medium。価値の本体はプロバイダ別正規化レシピのデータ整備。シークレット入力前提で必然性が高い  |
| A2-9 |         | **パスワードハッシュ / KDF ベンチ**      | `password-hash-lab`          | bcrypt / argon2id / scrypt / PBKDF2 の生成・検証＋コストパラメータごとの実測ベンチ。OWASP 推奨値とのギャップ警告、htpasswd 行生成                | `hash-wasm` で Low〜Medium。実パスワード・本番 DB のハッシュを入力するため外部送信不可。実測レイテンシはローカル実行が必須  |

## B tier ― 独自性・実用性あり

| #     | 状態 | ツール名                             | slug（案）                | 概要                                                                                                            | 技術メモ                                                                                                        |
| ----- | ---- | ------------------------------------ | ------------------------- | --------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| B2-1  |      | **DDL → ER 図ジェネレータ**          | `ddl-er-diagram`          | `CREATE TABLE` 文（MySQL/PostgreSQL）から ER 図を即描画。FK から自動でリレーション線、Mermaid / SVG 出力        | 本番スキーマは社外秘の塊。`node-sql-parser` + `mermaid`。SQL 整形ツールとシナジー。Medium                       |
| B2-2  |      | **CSV 突合・差分（キーマッチング）** | `csv-reconciler`          | 2つの CSV をキー列指定で突合（全角半角等の正規化込み）し、存在差・値差を色分け表示、結果を CSV 出力             | VLOOKUP 職人芸の置換。`papaparse` + Web Worker で数十万行対応。Medium                                           |
| B2-3  |      | **和暦⇄西暦一括変換**                | `wareki-converter`        | 「R6.4.1」「H31/4/30」等の表記ゆれをパースして西暦 ISO へ、CSV 日付列の一括変換、改元境界・存在しない日付の検証 | 元号テーブル＋パーサ自作、逆方向は `Intl.DateTimeFormat('ja-JP-u-ca-japanese')`。Low〜Medium                    |
| B2-4  |      | **チェックディジット検証・計算機**   | `check-digit`             | 法人番号 / ISBN / Luhn / 運転免許証番号等の検証・計算・CSV 一括検証・形式的に正しいダミー番号の生成             | 公開アルゴリズムの純ロジックで Low。JAN コード生成の知見を流用。マイナンバーは検証・テスト生成に限定            |
| B2-5  |      | **固定長ファイル⇄CSV 変換**          | `fixed-length-converter`  | レイアウト定義（開始位置 / バイト長 / 型）で Shift_JIS 固定長ファイルを CSV/JSON へ展開、逆方向も対応           | 全銀・COBOL 連携現場ど真ん中で Web ツールほぼ皆無。`encoding-japanese` でバイト長計算。Medium〜High             |
| B2-6  |      | **全銀フォーマットビューア**         | `zengin-viewer`           | 総合振込・口座振替レコードの構造表示・金額/件数トレーラ整合チェック・CSV からの生成。銀行コード辞書同梱         | `zengin-code`（MIT の JSON 辞書）。B2-5 と共通基盤を共有。ニッチ独占。Medium                                    |
| B2-7  | ✅   | **CSR・鍵ペアジェネレータ**          | `csr-generator`           | ブラウザ内で RSA/ECDSA 鍵ペアを生成し PKCS#10 CSR を出力（秘密鍵はローカル保存のみ）。既存 CSR の解析も         | 第1回 S-2（証明書解析＝読む側）の「作る側」。`pkijs` + Web Crypto。Medium                                       |
| B2-8  |      | **JWS/JWE プレイグラウンド**         | `jose-playground`         | 鍵ペア生成→署名→検証、JWE 暗号化/復号、JWKS 貼り付け検証・lint。alg confusion の再現実験モード                  | 既存 JWT デコーダ（閲覧専用）の対になる「作る・検証する側」。`jose`（panva）。Medium                            |
| B2-9  |      | **IAM ポリシー Lint＆要約**          | `iam-policy-lint`         | AWS IAM ポリシー JSON の危険パターン（`*` 権限・`NotAction`・`iam:PassRole`）検出と日本語要約、実効差分表示     | ARN から社内構成が透けるため外部送信不可。検知器のため test-gates 必須。Medium〜High                            |
| B2-10 |      | **DNS レコード静的 Lint**            | `dns-record-lint`         | SPF（lookup 数 10 制限の静的カウント）/ DKIM / DMARC の貼り付け式構文検証と日本語解説。DoH 照会はしない         | 「オフライン静的 lint」は空白地帯（既存は全てオンライン照会型）。CIDR 計算機ロジック再利用。Medium              |
| B2-11 |      | **nginx location シミュレータ**      | `nginx-location-tester`   | nginx 設定とテスト URL から、どの location が選ばれるかを優先順位ルールで判定・ハイライト。rewrite トレース     | nginx 最頻出のハマりどころでオフラインシミュレータ不在。正規表現ビジュアライザとシナジー。Medium〜High          |
| B2-12 |      | **フォント検査＆サブセット化**       | `font-inspector`          | woff2/ttf のグリフ・日本語水準カバレッジ・OpenType feature を可視化し、指定テキストのサブセット woff2 を生成    | ライセンスフォントは外部アップロード厳禁。`opentype.js` + `harfbuzzjs` + `wawoff2`（WASM）。High                |
| B2-13 |      | **ファビコン・PWA アセット一括生成** | `favicon-generator`       | 画像1枚から favicon.ico / apple-touch-icon / maskable icon / manifest 断片と `<head>` タグ一式を生成            | realfavicongenerator はサーバ型。未発表ロゴを送信しない＋maskable セーフゾーン表示。Canvas。Low〜Medium         |
| B2-14 |      | **CSS 詳細度・カスケード診断**       | `css-specificity`         | セレクタ詳細度の算出ソートに加え、`@layer` / `:is()` / `:where()` 込みで「なぜこのスタイルが勝つか」を診断      | 本リポジトリ自身の Tailwind v4 `@layer` 課題（`.agents/rules/common.md` 7.1 章）と直結する dogfooding 価値。Low |
| B2-15 |      | **OKLCH カラースタジオ**             | `color-studio`            | HEX/RGB/HSL/OKLCH/P3 相互変換・ガマットマッピング可視化・色覚シミュレーション並列表示・CSS 変数出力             | `culori` / `colorjs.io`。第1回 A-4（コントラスト比マトリクス）と将来連携。Medium                                |
| B2-16 |      | **営業日・祝日計算機**               | `business-day-calculator` | 日本の祝日をロジック計算し「N 営業日後」「月末営業日」「五十日」を計算。独自休業日の追加対応                    | `holiday_jp` または天文計算式の自作。第1回 A-3（Cron）と将来連携。Low〜Medium                                   |
| B2-17 |      | **電話番号・郵便番号正規化**         | `phone-postal-normalizer` | 市外局番に応じたハイフン位置補正・固定/携帯/フリーダイヤル種別判定・桁数異常検出を CSV 列一括で                 | 顧客名簿そのものを扱うため外部送信不可。`libphonenumber-js` + 総務省市外局番表の辞書化。Medium                  |
| B2-18 |      | **日本語ダミー個人データ生成**       | `dummy-personal-data`     | 日本人氏名（漢字＋フリガナ整合）・架空住所・実在しない帯域の電話番号・和暦生年月日を指定件数 CSV/JSON 生成      | S2-3（匿名化）と対になる生成側。自前辞書同梱（faker の ja ロケールはバンドル過大）。Low〜Medium                 |

## C tier ― ニッチ・実装重量級・既存代替あり（短評のみ）

| #     | ツール名                                         | 備考                                                                                                          |
| ----- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------- |
| C2-1  | 対称暗号プレイグラウンド（AES 相互運用デバッガ） | IV 連結・タグ位置等の復号失敗診断付き。CyberChef と競合するが診断観点で差別化。Web Crypto のみ。Medium        |
| C2-2  | WebAuthn / パスキーデバッガ                      | attestationObject（CBOR）の生バイト構造可視化。ブラウザ API そのものだが webauthn.io が存在。High             |
| C2-3  | pcap パケットキャプチャビューア                  | ブラウザ完結 pcap は空白だが High。ヘッダレベル限定に絞れば Medium〜High。ネットワーク系の目玉候補            |
| C2-4  | 動画トリミング＆GIF/WebM 変換                    | バグ報告 GIF 特化（画面録画は機密映り込み前提）。WebCodecs + `mp4box.js`。High                                |
| C2-5  | Shamir 秘密分散                                  | k-of-n 分割・QR 出力。既存 QR 生成/リーダー資産と相乗。airgap 運用と相性最高だがニッチ。Medium                |
| C2-6  | トークン・乱数品質チェッカー                     | 発行済みトークン群のエントロピー・系列相関を統計検定。ULID/UUID ツールの文脈に接続。Medium                    |
| C2-7  | 暗号設定 Lint（TLS/SSH 設定）                    | `ssl_ciphers` 等を CRYPTREC / Mozilla 推奨と突き合わせ。接続型スキャナの補集合だが辞書整備が重い。Medium      |
| C2-8  | イージングエディタ                               | cubic-bezier 編集＋スプリング→CSS `linear()` 生成。cubic-bezier.com と競合、`linear()` で差別化。Medium       |
| C2-9  | アクセシブルネーム＆a11y ツリー検査              | HTML 断片の computed role / accname を検査。`dom-accessibility-api`。本プロジェクトの a11y 方針と整合。Medium |
| C2-10 | gzip/brotli 圧縮サイズ見積もり                   | `CompressionStream` 組込み API で Low。bundlephobia が対象外とする「手元の任意コード片」を扱える              |
| C2-11 | curl ⇄ コード変換                                | `curlconverter` がブラウザ動作するが、本家がすでにクライアントサイドで差別化弱。Medium                        |
| C2-12 | レスポンシブ画像 srcset ヘルパー                 | 生成＋実ブラウザの `currentSrc` 検証。ニッチ。Low〜Medium                                                     |
| C2-13 | Excel シリアル値⇄日付変換                        | 1900/1904 両体系・うるう年バグ対応。第1回 C-1（UNIX タイムスタンプ変換）への統合が妥当。Low                   |
| C2-14 | カナ⇄ヘボン式ローマ字変換                        | 口座名義・パスポート表記規則（撥音 M 化・長音省略）。A2-2 への統合も検討。Low                                 |
| C2-15 | メール生ヘッダ解析                               | Received チェーン可視化・認証結果解読。`postal-mime`。B2-10（DNS Lint）との統合余地。Medium                   |
| C2-16 | ローカルフォルダ シークレットスキャナ            | File System Access API でフォルダ走査。Chromium 限定が難点。S2-1 のスコープ拡張として検討。Medium             |

## 重複・統合メモ（第2回）

- **マスキング系の統合**: S2-1（非構造化テキスト/ログ）と S2-3（CSV 等の構造化データ）は検出正規表現・ダミー値辞書を共有できる。実装時は共通基盤（検出ルールセット＋一貫トークン化）を `src/utils/` に切り出してから 2 本に分けるのが効率的。C2-16 は S2-1 の入力手段拡張。
- **CSV 列処理基盤**: S2-3 / A2-3 / B2-2 / B2-3 / B2-4 / B2-17 は「CSV 読込 → 列選択 → 処理 → CSV 出力」の同型構造。`papaparse` + Web Worker + 列選択 UI を共通コンポーネント化すると 2 本目以降の追加コストが激減する。
- **鍵・証明書系列**: A2-7 / B2-7 / 第1回 S-2 は PEM/DER パース基盤（`pkijs`）を共有するシリーズとして設計できる。
- **KDF**: セキュリティ視点の「KDF プレイグラウンド」案は A2-9 に統合（パラメータベンチ＋他言語相互運用チェックを内包）。
- **SAML**: バックエンド・セキュリティ両視点の案を S2-2 に統合。初版は「デコード＋構造表示＋定番チェックリスト」に絞り、XMLDSig 署名検証（C14N が山場）は第2版で検討。
- **検知器の実装規約**: S2-1 / A2-6 / B2-9 / B2-10 など「検出・警告」を持つツールは `.agents/rules/common.md` 3 章に従い **test-gates skill による陽性対照テストが必須**。
